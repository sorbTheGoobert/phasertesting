// const { Phaser } = require("./phaser.min.js");

// window.onload = function() {
    var game = new Phaser.Game(config);
// }
var config = {
    width: 800,
    height: 800,
    backgroundColor: "#000",
    scene: [Scene1, Scene2]
}